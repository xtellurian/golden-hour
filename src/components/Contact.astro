---
// Contact Section Component
---

<section id="contact" class="contact">
	<div class="container">
		<div class="contact-content">
			<div class="contact-info">
				<h2>Get In Touch</h2>
				<p>
					Ready to learn more about how we can support your family? 
					We'd love to hear from you and discuss how our services can meet your needs.
				</p>
				
				<div class="contact-details">
					<div class="contact-item">
						<strong>Email</strong>
						<p><a href="mailto:ellie@thegoldenhourco.net">ellie@thegoldenhourco.net</a></p>
					</div>
					<div class="contact-item">
						<strong>Phone</strong>
						<p><a href="tel:+61466499868">0466 499 868</a></p>
					</div>
					<div class="contact-item">
						<strong>Service Area</strong>
						<p>Fremantle and Perth Metropolitan Area</p>
					</div>
					<div class="contact-item">
						<strong>Location</strong>
						<p>Fremantle, Western Australia</p>
					</div>
				</div>
			</div>
			
			<div class="contact-cta">
				<p class="contact-message">
					Contact us today to schedule a consultation or ask any questions about our services.
				</p>
				<a href="mailto:ellie@thegoldenhourco.net" class="button">Send Us an Email</a>
			</div>
		</div>
	</div>
</section>

<style>
	.contact {
		padding: var(--spacing-2xl) 0;
		background-color: var(--color-white);
	}
	
	.contact-content {
		display: grid;
		gap: var(--spacing-xl);
	}
	
	.contact-info h2 {
		color: var(--color-primary-dark);
		margin-bottom: var(--spacing-md);
	}
	
	.contact-details {
		display: grid;
		gap: var(--spacing-md);
		margin-top: var(--spacing-lg);
	}
	
	.contact-item strong {
		display: block;
		color: var(--color-primary);
		margin-bottom: var(--spacing-xs);
	}
	
	.contact-item p {
		color: var(--color-text-light);
		margin: 0;
	}
	
	.contact-cta {
		background: var(--color-background);
		padding: var(--spacing-lg);
		border-radius: 8px;
		border: 1px solid #E8E2D5;
		text-align: center;
	}
	
	.contact-message {
		font-size: 1.125rem;
		color: var(--color-text-light);
		margin-bottom: var(--spacing-lg);
	}
	
	@media (min-width: 768px) {
		.contact-content {
			grid-template-columns: 1fr 1fr;
			align-items: start;
		}
		
		.contact-info {
			padding-right: var(--spacing-lg);
		}
	}
</style>
